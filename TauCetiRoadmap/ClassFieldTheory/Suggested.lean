import Mathlib

    /-!
    # Suggested declarations for class field theory: abstract formalism, local fields, and number fields

    This file is an identifier ledger for the draft roadmap.  The accompanying
    `README.md` is the specification.  The declarations below should be replaced
    by genuine prototypes, against the accepted supplier APIs, before the roadmap
    is submitted for final review.

    Principal names to prototype:

    - ``tateH`, Tate restriction/corestriction/inflation, and `ordinaryToTate``
- ``FiniteClassFormation`, `ClassFormation`, `fundamentalClass`, and `tateNakayama``
- ``localArtinMap`, `localNormResidue`, `localInvariant`, and `localTateDuality``
- ``globalArtinMap`, `globalReciprocity`, `classFieldExistence`, and `hilbertProductFormula``
- ``hilbertClassField`, `rayClassField`, `ringClassField`, and `kroneckerWeber``
    -/

    namespace TauCetiRoadmap.ClassFieldTheory

    -- Deliberately empty at the portfolio-draft stage.  Do not use `Prop := sorry`
    -- placeholders for conditions whose mathematical type has not yet been built.

    end TauCetiRoadmap.ClassFieldTheory
