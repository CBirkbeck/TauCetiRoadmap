import Mathlib

    /-!
    # Suggested declarations for adelic algebraic groups, strong approximation, and Tamagawa measures

    This file is an identifier ledger for the draft roadmap.  The accompanying
    `README.md` is the specification.  The declarations below should be replaced
    by genuine prototypes, against the accepted supplier APIs, before the roadmap
    is submitted for final review.

    Principal names to prototype:

    - ``finiteAdelicPoints`, `adelicPoints`, `integralCompactOpen``
- ``tamagawaMeasure` and its choice-independence theorem`
- ``StrongApproximationAt` and the simply connected strong-approximation theorem`
- `central-isogeny comparison and quotient-volume formulae`
    -/

    namespace TauCetiRoadmap.AdelicAlgebraicGroups

    -- Deliberately empty at the portfolio-draft stage.  Do not use `Prop := sorry`
    -- placeholders for conditions whose mathematical type has not yet been built.

    end TauCetiRoadmap.AdelicAlgebraicGroups
