import Mathlib

    /-!
    # Suggested declarations for global number fields, adeles, ray class groups, and Hecke characters

    This file is an identifier ledger for the draft roadmap.  The accompanying
    `README.md` is the specification.  The declarations below should be replaced
    by genuine prototypes, against the accepted supplier APIs, before the roadmap
    is submitted for final review.

    Principal names to prototype:

    - ``Modulus`, `rayClassGroup`, `narrowRayClassGroup`, and their exact sequences`
- ``finiteAdeleRing`, `ideleGroup`, `ideleClassGroup`, and the product formula`
- ``RayClassCharacter`, `HeckeCharacter`, `IsPrimitive`, and conductor operations`
- ``NumberFieldOrder`, `properIdeals`, `Pic`, and `NarrowPic``
    -/

    namespace TauCetiRoadmap.GlobalNumberFields

    -- Deliberately empty at the portfolio-draft stage.  Do not use `Prop := sorry`
    -- placeholders for conditions whose mathematical type has not yet been built.

    end TauCetiRoadmap.GlobalNumberFields
